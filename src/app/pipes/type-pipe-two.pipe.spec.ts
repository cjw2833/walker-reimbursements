import { TypePipeTwoPipe } from './type-pipe-two.pipe';

describe('TypePipeTwoPipe', () => {
  it('create an instance', () => {
    const pipe = new TypePipeTwoPipe();
    expect(pipe).toBeTruthy();
  });
});
