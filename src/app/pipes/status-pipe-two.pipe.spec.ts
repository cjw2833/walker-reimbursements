import { StatusPipeTwoPipe } from './status-pipe-two.pipe';

describe('StatusPipeTwoPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusPipeTwoPipe();
    expect(pipe).toBeTruthy();
  });
});
